{% extends 'base.html.twig' %}

{% block title %}Hello DataController!{% endblock %}

{% block main %}
    {% for movie in movies|sort((a, b) => b.rating <=> a.rating) %}
        <p>{{ movie.name }}: {{ movie.rating }}</p>
    {% endfor %}
{% endblock %}
